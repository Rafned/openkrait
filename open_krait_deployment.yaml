apiVersion: apps/v1
kind: Deployment
metadata:
  name: open_krait
spec:
  replicas: 1
  selector:
    matchLabels:
      app: open_krait
  template:
    metadata:
      labels:
        app: open_krait
    spec:
      containers:
      - name: open_krait
        image: your-username/open_krait:1.0.0
        env:
        - name: VAULT_TOKEN
          valueFrom:
            secretKeyRef:
              name: vault-secret
              key: token
        volumeMounts:
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: logs
        emptyDir: {}
---
apiVersion: v1
kind: Secret
metadata:
  name: vault-secret
type: Opaque
data:
  token: <base64-encoded-vault-token>
